<div class='top-function-menu rounded'>
  <%= link_to t('word.new'), word_path(:action => 'new'), {:popup => true} %>
</div>
<table>
  <thead>
    <th class='wh-100'>提供人</th>
    <th>佳句</th>
  </thead>
  <% @words.each do |word| %>
    <tr class='<%= cycle("bg-ly", "") %>'>
      <% user = word.user %>
      <td><%= link_to_user user, user.name %></td>
      <td><%= format_content word.content %></td>
    </tr>
  <% end %>
</table>
<%= will_paginate @words %>
